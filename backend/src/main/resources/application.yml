spring:
  application:
    name: backend
  datasource:
    url: jdbc:mysql://localhost:3306/TixheDecor
    username: root
    password: root123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

jwt:
  secret: dekorimi_secret_key_2024_very_long_string_minimum_32_chars
  expiration: 86400000

server:
  port: 8080